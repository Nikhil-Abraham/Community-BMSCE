const User = require("../models/user.model");
const router = require("express").Router();

// Upddate User
router.get("/", (req, res) => {
  res.send("user route");
});

// Delete UserData
// Get User
// Follow a user
// Unfollow a user

module.exports = router;
